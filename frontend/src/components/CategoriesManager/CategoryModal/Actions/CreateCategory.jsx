// /CategoryModal/actions/CreateCategory.jsx
const CreateCategory = ({ onClose }) => {
  return (
    <div>
      <h2>Create New Category</h2>
      {/* Add creation form here later */}
      <button onClick={onClose}>Close</button>
    </div>
  );
};

export default CreateCategory;
